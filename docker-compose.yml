services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csu
      POSTGRES_PASSWORD: csu_dev_pw
      POSTGRES_DB: csu_portal
    ports:
      - "5432:5432"
    volumes:
      - csu_pgdata:/var/lib/postgresql/data

volumes:
  csu_pgdata:
